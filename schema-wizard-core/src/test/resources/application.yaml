schema:
  wizard:
    database:
      connection-url: jdbc:postgresql://${DS_HOST:default}:${DS_PORT:5432}/${DS_DATABASE:changeMe}?reWriteBatchedInserts=true
      username: postgres
      password: postgres
    migration:
      package-name: com.example.db.migration
    log:
      sql-query: false
