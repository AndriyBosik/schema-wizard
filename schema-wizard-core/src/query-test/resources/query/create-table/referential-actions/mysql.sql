CREATE TABLE IF NOT EXISTS posts (email VARCHAR(50) NOT NULL, id INTEGER NOT NULL, user_id INTEGER NULL, PRIMARY KEY (id), CONSTRAINT fk_posts_user_id FOREIGN KEY (user_id) REFERENCES schemawizard.users (id) ON DELETE CASCADE ON UPDATE SET NULL)